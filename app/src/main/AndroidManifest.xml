<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="info.androidhive.bottomnavigation">

    <uses-permission android:name="android.permission.INTERNET" />


    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name=".app.MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="PROBSO"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        android:largeHeap="true"
        <activity
            android:name=".Splashscreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".ForgetAndChangePasswordActivity" />
        <activity android:name=".BottomNavigation" />
        <activity android:name=".PostActivity" />
        <activity android:name=".RecycleronClick" />
        <activity android:name=".PostSignupActivity" />
        <activity android:name=".SendReplyActivity" />
        <activity android:name=".MessageUseronClick" />
        <activity android:name=".AuthBottomNavigation" />
        <activity android:name=".AuthLoginActivity" />
        <activity android:name=".SocialPostActivity" />
    </application>

</manifest>